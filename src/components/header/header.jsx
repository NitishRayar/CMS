import React, { Fragment } from "react";
import "./header.css";

const Header = (props) => {
  return (
    <Fragment>
      <header className="header">
        <h1>CMS</h1>
      </header>
    </Fragment>
  );
};

export default Header;
