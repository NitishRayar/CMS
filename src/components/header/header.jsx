import React, { Fragment } from "react";
import "./header.css";

const Header = (props) => {
  return (
    <Fragment>
      <header className="header">
        <h1>M R Patil</h1>
      </header>
    </Fragment>
  );
};

export default Header;
